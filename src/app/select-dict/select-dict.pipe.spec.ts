import { SelectDictPipe } from './select-dict.pipe';

describe('SelectDictPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectDictPipe();
    expect(pipe).toBeTruthy();
  });
});
